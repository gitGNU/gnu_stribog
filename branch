trunc<autobuild

trunc branch
main tasks are:

Mounting the project on the GNU build system

Parsing the command line arguments of the host tools in
a GNU-standard way

Fixing some usability issues before forking the first release branch
